package GUI.Buttons;

import javafx.scene.control.Button;

/**
 * Class that creates button to be linked to new window creation.
 */
public class NewWindowButton extends Button {
    public NewWindowButton(String title){
        super(title);
    }
}
